extend type Mutation {
    createTeam(input: CreateTeamInput! @spread): TeamMutationResponse!
        @field(
            resolver: "App\\GraphQL\\Mutations\\CreateTeamMutation@createTeam"
        )
}
