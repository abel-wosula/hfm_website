extend type Mutation {
    createUpload(input: CreateUploadInput! @spread): UploadMutationResponse!
        @field(
            resolver: "App\\GraphQL\\Mutations\\CreateUploadMutation@createUpload"
        )
}
